#polynomial:An algebraic expression consisting of variables raised to non-negative integer powers and multiplied by coefficients.
import numpy as np
p = np.poly1d([1, 2, 3])  # Creates a polynomial: x^2 + 2x + 3
print(p)